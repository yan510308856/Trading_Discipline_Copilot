import { PageIntro } from "./PageIntro";

export function TradeChecklist() {
  return (
    <PageIntro
      eyebrow="Pre-trade"
      title="New Trade Checklist"
      description="Turn a trade idea into a deliberate plan before risk enters the market."
      nextStage="The controlled planning form and live rule evaluation arrive in Stage 5."
    />
  );
}
