import { PageIntro } from "./PageIntro";

export function PostTradeReview() {
  return (
    <PageIntro
      eyebrow="Review"
      title="Post-Trade Review"
      description="Separate execution quality from P&amp;L and carry one useful lesson forward."
      nextStage="Discipline scoring and review inputs are reserved for Stage 7."
    />
  );
}
