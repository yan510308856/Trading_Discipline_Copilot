import { PageIntro } from "./PageIntro";

export function RulesLibrary() {
  return (
    <PageIntro
      eyebrow="Reference"
      title="Rules Library"
      description="Browse the discipline principles that power pre-trade and in-trade alerts."
      nextStage="Searchable YAML-backed rule cards are reserved for Stage 9."
    />
  );
}
